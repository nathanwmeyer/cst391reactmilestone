import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'

// Start with: "npm start"
// this is the start of the application, this file renders the App
ReactDOM.render(<App />, document.querySelector("#root"));